<h2>Usage</h2>
<p><%= number_to_human_size(dataset.used) %> / <%= number_to_human_size(dataset.available) %> <span class="pull-right"><%= percentage_used(dataset) %>%</span></p>
<div class="progress progress-thin">
  <div class="progress-bar" role="progressbar" aria-valuenow="40" aria-valuemin="0" aria-valuemax="100" style="width: <%= percentage_used(dataset) %>%"></div>
</div>

<p>Children: <span class="pull-right"><%= number_to_human_size(dataset.usedbychildren) %></span></p>
<p>Snapshots: <span class="pull-right"><%= number_to_human_size(dataset.usedbysnapshots) %></span></p>